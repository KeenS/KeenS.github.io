<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Slides on κeenのHappy Hacκing Blog</title>
    <link>https://KeenS.github.io/slide/</link>
    <description>Recent content in Slides on κeenのHappy Hacκing Blog</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>ja-jp</language>
    <lastBuildDate>Sun, 27 Nov 2022 18:53:38 +0900</lastBuildDate>
    
	<atom:link href="https://KeenS.github.io/slide/index.xml" rel="self" type="application/rss+xml" />
    
    
    <item>
      <title>Rustの非同期デバッグツールを使いこなせ!</title>
      <link>https://KeenS.github.io/slide/rustnohidoukidebaggutsu_ruwotsukaikonase_/</link>
      <pubDate>Sun, 27 Nov 2022 18:53:38 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/rustnohidoukidebaggutsu_ruwotsukaikonase_/</guid>
      <description>TechFeed Experts Night#9 〜 Rust/WebAssemblyの「いま」を探るでの発表用。tokioから出ている非同期プログラムのデバッグツール(ライブラリ)群の紹介</description>
    </item>
    
    <item>
      <title>Wasmでできること、できそうなこと</title>
      <link>https://KeenS.github.io/slide/wasmdedekirukoto_dekisounakoto/</link>
      <pubDate>Wed, 02 Nov 2022 21:31:40 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/wasmdedekirukoto_dekisounakoto/</guid>
      <description>みんなのPython勉強会での発表用。比較的低レベルなWasmの入門</description>
    </item>
    
    <item>
      <title>関数型データ構造と依存型同時入門</title>
      <link>https://KeenS.github.io/slide/kansuugatade_takouzoutoizongatadoujinyuumon/</link>
      <pubDate>Tue, 20 Sep 2022 14:27:56 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/kansuugatade_takouzoutoizongatadoujinyuumon/</guid>
      <description>第三回関数型プログラミング（仮）の会 (https://opt.connpass.com/event/255865/)での発表用。Banker&#39;s Queueの紹介と依存型の紹介</description>
    </item>
    
    <item>
      <title>DropとFutureとDropのfuture</title>
      <link>https://KeenS.github.io/slide/droptofuturetodropnofuture/</link>
      <pubDate>Sat, 14 May 2022 16:36:25 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/droptofuturetodropnofuture/</guid>
      <description>tfconでの発表用</description>
    </item>
    
    <item>
      <title>Idris 2の数量的型が解決した問題、導入した問題</title>
      <link>https://KeenS.github.io/slide/idris_2nosuuryoutekikatagakaiketsushitamondai_dounyuushitamondai/</link>
      <pubDate>Sun, 26 Sep 2021 16:53:22 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/idris_2nosuuryoutekikatagakaiketsushitamondai_dounyuushitamondai/</guid>
      <description>関数型言語Idrisの次世代版として開発されているIdris 2の大きな特徴の1つとして型システムに数量的型理論（Quantitative Type Theory）を導入していることが挙げられます。このシステムではIdris 1にあった証明環境での利用可能な値の問題や、本来ならば実行時に不要な型を持ってしまっていた問題を解決します。一方で数量的型を便利に使うための仕組みであった多重度の部分型付けにより非健全性が入るなどの問題もありました。本発表ではIdris 2の数量的型のそういった特徴を紹介します。</description>
    </item>
    
    <item>
      <title>PLDI論文読み会: Perceus: Garbage Free Reference Counting with Reuse</title>
      <link>https://KeenS.github.io/slide/pldironbunyomikai__perceus__garbage_free_reference_counting_with_reuse/</link>
      <pubDate>Fri, 17 Sep 2021 00:55:32 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/pldironbunyomikai__perceus__garbage_free_reference_counting_with_reuse/</guid>
      <description>社内勉強会のPLDI論文読み会で発表した資料です</description>
    </item>
    
    <item>
      <title>Rust with Fearless Concurrency</title>
      <link>https://KeenS.github.io/slide/rust_with_fearless_concurrency/</link>
      <pubDate>Sun, 15 Nov 2020 15:54:34 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/rust_with_fearless_concurrency/</guid>
      <description>&lt;textarea data-markdown
    data-separator=&#34;\n===\n&#34;
    data-vertical=&#34;\n---\n&#34;
    data-notes=&#34;^Note:&#34;&gt;
# Rust with Fearless Concurrency
----------------------

&lt;!-- .slide: class=&#34;center&#34; --&gt;

===
# About Me
---------
![κeenのアイコン](/images/kappa2_vest.png) &lt;!-- .element: style=&#34;position:absolute;right:0;z-index:-1&#34; width=&#34;20%&#34; --&gt;

* κeen
* [@blackenedgold](https://twitter.com/blackenedgold)
* GitHub: [KeenS](https://github.com/KeenS)
* GitLab: [blackenedgold](https://gitlab.com/blackenedgold)
* [Idein Inc.](https://idein.jp/)のエンジニア
* Lisp, ML, Rust, Shell Scriptあたりを書きます

===
# Rust with Fearless Concurrency
---------------------------------

* 前半Rustの紹介
* 後半RustのFearless Concurrencyについて

Note:

普段のRustのイントロだと言語機能の紹介だけで50分まるまる使う。
今回はConcurrencyに言及してほしいという依頼だったので言語機能の紹介が少なめ

===
# Rustとは
------------

* 比較的最近できた **システムプログラミング言語**
  + 2015年に1.0リリース
  + C/C++並に[速い](https://benchmarksgame-team.pages.debian.net/benchmarksgame/which-programs-are-fastest.html)
* システムソフトウェアを作るのに便利
  + [OS](https://redox-os.org), [ブラウザ](https://servo.org), [DB](https://www.influxdata.com/blog/announcing-influxdb-iox/)など
* Rustは「安全」
  + 未定義動作がない

===

# Rustは安全
------------

* ざっくり言うとC/C++でいう未定義動作がない
* メモリ安全
  + Null PointerやDangling Pointerなど発生しない機構
  + FYI: [「Chrome」の深刻なセキュリティ脆弱性、70％はメモリー安全性の問題](https://japan.zdnet.com/article/35154338/)
* データ競合がない
  + 今日の後半の話
* などなど

===
# 安全である価値
----------------

* 重要なパーツからRustに置き換えられていく流れがきている
* マイクロソフトがWindowsのコンポーネントの一部にRustを使うことを[検討](https://msrc-blog.microsoft.com/2019/11/07/using-rust-in-windows/)
* Amazonのセキュリティコンテナの実装に[使われてる](https://github.com/firecracker-microvm/firecracker/)
* Rust製TLSライブラリの監査の結果[高評価](https://github.com/ctz/rustls/blob/master/audit/TLS-01-report.pdf)
* cURLにRustバックエンド追加の[計画](https://www.abetterinternet.org/post/memory-safe-curl/)
  + ついでに↑のRust製TLSライブラリも

===
# Hello, Rust
--------------


[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=811e91c9fd9230f2c2d1db1182abe1ac)

``` rust
fn main() {
    println!(&#34;Hello, Rust&#34;);
}
```

===
# FizzBuzz
----------

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=7e5653ef7b1b7fb2cb292ed22f448d56)

``` rust
fn fizz_buzz(n: u64) {
    for i in 0..n {
        match (i % 3, i % 5) {
            (0, 0) =&gt; println!(&#34;FizzBuzz&#34;),
            (0, _) =&gt; println!(&#34;Fizz&#34;),
            (_, 0) =&gt; println!(&#34;Buzz&#34;),
            _      =&gt; println!(&#34;{}&#34;, i),
        }
    }
}
```

===
# Sum?
------

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=3eda2f981b8a001d791983e912f468cb)

``` rust
fn sum(vec: Vec&lt;i64&gt;) -&gt; i64 {
    let result = 0;
    for e in vec {
        result += e;
    }
    return result;
}
```

===
# Sum
------

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=0f6bfff337d14d071e309052ee1f01f9)

``` rust
fn sum(vec: Vec&lt;i64&gt;) -&gt; i64 {
    let mut result = 0;
    for e in vec {
        result += e;
    }
    return result;
}
```

===
# Sum!
------

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=8c040eb82c4c117d103f211bf109c17e)

``` rust
fn sum(vec: Vec&lt;i64&gt;) -&gt; i64 {
    vec
      .into_iter()
      .fold(0, |e, acc| e + acc)
}
```

Note:
returnがない

===
# Rustの特徴
------------

* MLやHaskellなどの関数型言語から影響を受けた
  + 変数が不変だったり式指向だったり
  + 代数的データ型とパターンマッチなど
* `obj.method()` などの構文はあるが、クラスがない
* メモリを自動で管理しつつもGCなどのランタイムがない

===

# 構造体
---------
* クラスと継承はない
  + 継承は最近はアンチパターンとされつつある（要出典）

``` rust
struct Point {
    x: f32,
    y: f32,
}

struct Rectangle {
    top_left: Point,
    bottom_right: Point,
}

```

===
# 構造体のメモリレイアウト
--------------------------

ポインタを挟まず `f32` が4つ並ぶ

``` text
Rectangle
+-----+-----+-----+-----+
| f32 | f32 | f32 | f32 |
+-----+-----+-----+-----+
```

===

# 構造体とメソッドの例
---------------------

``` rust
impl Rectangle {
    fn space(&amp;self) -&gt; f32 {
        let width = self.bottom_right.x - self.top_left.x;
        let height = self.top_left.y - self.bottom_right.y;
        width * height
    }
}

let rect = Rectangle {
    top_left: Point { x: 0.0, y: 0.0 },
    bottom_right: Point { x: 200.0, y: 100.0 },
};
rect.space();
```

===
# 列挙型
--------

* 可能な値を列挙できる

from [std::net::Shutdown](https://doc.rust-lang.org/std/net/enum.Shutdown.html)

``` rust
pub enum Shutdown {
    Read,
    Write,
    Both,
}
```

===
# 複雑な列挙型
--------

* 列挙子は値を持てる
  + Javaでいう抽象クラス+継承相当？

from [std::net::IpAddr](https://doc.rust-lang.org/std/net/enum.IpAddr.html)

``` rust
pub enum IpAddr {
    V4(Ipv4Addr),
    V6(Ipv6Addr),
}
```

===
# Option
---------

* Rustにnullはない
  + `Option&lt;T&gt;` を使う

``` text
// &lt;T&gt; はジェネリクス
enum Option&lt;T&gt; {
    Some(T),
    None
}
```

===
# Optionの利用例
----------------

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=bf778a36c19e40b4227c66ae2c8a607a)

``` rust
impl UserDb {
    fn find(&amp;self, id: &amp;u64) -&gt; Option&lt;&amp;User&gt; {
        self.db.get(&amp;id)
    }
}

match db.find(&amp;1) {
    Some(user) =&gt; println!(&#34;User found&#34;),
    None =&gt; println!(&#34;User not found&#34;),
}
```


===
# Result
--------

* Rustに例外はない
  + `Result&lt;T, E&gt;` を使う

``` rust
enum Result&lt;T, E&gt; {
    Ok(T),
    Err(E)
}
```

===
# Resultの利用例
----------------

* 「例外が投げられる」という了解的ものだったのが第一級市民となることで扱いやすくなった

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=4e483d5a76a6ba0bf840200921cc07bc)

``` rust
let arg = env::args().nth(1);
let env = env::var(&#34;PARAM&#34;);
match (arg, env) {
    (Some(s), _) =&gt; println!(&#34;Param is given via arg: {}&#34;, s),
    (_, Ok(s)) =&gt; println!(&#34;Param is given via env: {}&#34;, s),
    _ =&gt; println!(&#34;No param given &#34;),
}
```

===
# 所有権
--------

* 値には唯一の所有者がいる
* 変数に代入すると所有権が移る
  + 関数呼び出しでも

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=83f71af33ce956c6fb222432288eba29)

``` rust
let x = vec![1, 2, 3];
let y = x;  //          &lt;-- 所有者が移った
// println!(&#34;{:?}&#34;, x); &lt;-- エラー
```

===
# ライフタイム
-------------

* スコープの末尾で値は開放される
  + デストラクタも呼ばれる

``` rust
let x = vec![1, 2, 3];
{
  let y = x;
  println!(&#34;{:?}&#34;, y);
  // ここでyが開放される
}
// ここではVecは開放済み
```


===
# 借用
------

* ポインタがある
  + 所有権は移動しない

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=da39ae24c347798067851a6276153f1c)

``` rust
let x = vec![1, 2, 3];
{
  let y = &amp;x; // &lt;- 借用
  println!(&#34;{:?}&#34;, y);
  // ここでyが開放される
  // つまりxに返却される
}

// 返してもらったあとはまた使える
println!(&#34;{:?}&#34;, x);
```


===
# 借用とライフタイム
-------------------

* 借用は元の値のライフタイムを越えられない

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=bf4a82438c38677914f4a6fc47909e23)

``` rust
fn return_ref() -&gt; &amp;i32 {
  let x = 1;
  &amp;x
  // &lt;- xはここで終了
}
```

===
# staticライフタイム
--------------------

* 他の値に縛られないライフタイムもある
  + 借用じゃない値とか
  + 文字列リテラルとか
* そういうライフタイルに `&#39;static` という名前がつく

``` rust
let s: &amp;&#39;static str = &#34;literal value&#34;;
```

===
# 参照の可変と不変
------------------

* 参照には可変と不変がある
* 可変な参照は1つしか存在しない
* 不変な参照は複数存在できる
  + つまり値を共有できる
* 可変な参照と不変な参照は同時に存在できない
* 上記制約を **コンパイル時に** 解決する

===
# 可変性エラー
-------------

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=e92ea6a7c5d9dcfbb046338c3de5668e)

``` rust
fn main() {
    // ベクトルを用意する
    let mut vec = vec![1, 2, 3];
    // ベクトルの要素への参照を取り出す。
    // ベクトルをイミュータブルに参照する
    for i in &amp;vec {
        // 既にベクトルはイミュータブルに参照されているので
        // ここでベクトルを変更しようとするとエラー
        vec.push(i * 2);
    }
}
```

===
# 所有権とRAII
----------------

* 所有権のおかげで自然にRAIIできる
  + Resource Acquisition Is Initialization
* リソースの開放はデストラクタに任せる
  + ライフタイムのおかげで勝手にデストラクタが走る
* `File` に `close` がない
  + 代わりにデストラクタを使う

===
# Fileとデストラクタ
--------------------

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=9785b579d020aca62392ac92b0da5e4c)

``` rust
let mut file = File::create(&#34;test.txt&#34;)?;
file.write_all(b&#34;Hello&#34;)?;
// ライフタイムが終わると自動で閉じられる
```

===
# ランタイム
------------

* Rustにはランタイム（GC）がない
  + 所有権のおかげ
  + システムプログラミング言語として重要
* パフォーマンス以外にも移植性が良
  + 組み込み
  + Webブラウザ(WebAssembly)

===
# 所有権の共有
--------------

* 所有権を共有したいケースもある
  + ロックとか
* Rustはいくつか所有権を共有する手段を提供している
  + 参照カウントとか

===
# `Rc`
-----

* 参照カウント（Reference Count）
* 所有権を共有する代表的手段
* `clone` でカウントを1増やしてデストラクタで1減らす
* カウントが0になったらメモリ開放
* マルチスレッドでは **使えない**
  + カウントを増減させる動作がスレッドセーフでない

===
# `Rc` の例
-----

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=d4127622f721f614f70f66fd9fbe6b7c)

``` rust
use std::rc::Rc;
let foo = Rc::new(vec![1.0, 2.0, 3.0]);
// The two syntaxes below are equivalent.
let a = foo.clone();
let b = Rc::clone(&amp;foo);
// a and b both point to the same memory location as foo.
```


===

# その他Rustの機能など
----------------------

* トレイト
  + インタフェースのようなもの
* クロージャ
* unsafe
  + FFI

===
# クロージャ
------------

* 匿名関数を作れる
* 環境にある変数を保存できる

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=d10ea3de7c89b00ed1addbdbe989e921)

``` rust
let mut x = 0;
|| {
  x +=1;
  x
}
```

===
# クロージャとライフタイム
------------

* 変数は借用するのでライフタイム制約を受ける
* moveを使うと変数の所有権を奪える

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=dba3806b12adeaa1a231c0c33f7ab888)

``` rust
fn counter() -&gt; impl FnMut() -&gt; u64 {
    let mut x = 0;
    || {
      x +=1;
      x
    }
}
```

``` rust
fn inc() -&gt; impl FnMut() -&gt; u64 {
    let mut x = 0;
    move || {
      x +=1;
      x
    }
}
```

===
# unsafe
--------

* Rustの制約を一時的に外すことができる
  + `unsafe {}` で囲む
* `unsafe` ブロックの中ではC並に安全でないことができる
  + NULL pointerとか色々

===
# unsafeなコード
---------------

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2015&amp;gist=6a04ca87dc616a2dfece0aae00e9e981)


```rust
use std::ffi::c_void;
use std::ptr::null_mut;

extern &#34;C&#34; {
  // FFIの関数のプロトタイプ宣言
  // 参照とは別のマジのポインタ型
  fn free(p: *mut c_void);
}

fn main() {
  // unsafeで囲むとやりたい放題
  unsafe {
    // ヌルポが作れる！！
    let p: *mut i32 = null_mut::&lt;i32&gt;();
    // ヌルポに書き込める！！
    *p = 1;
    // freeできる！！
    free(p.cast());
    // use after freeできる！！
    println!(&#34;{}&#34;, *p);
  }
}
```


===
# FFI
-----

* Cの関数を呼び出せる
* CにAPIを提供できる

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=96f62fd8635f09e4ba1b9eee071c148d)

``` rust
use libc::{suseconds_t, time_t};
use std::mem;
use std::os::raw::c_int;
use std::ptr;

// #[repr(C)]をつけることでCと相互運用できる型になる。
// メモリ上の表現がC互換になるというだけで、それ以外は普通のRustの構造体として扱える。
// struct timeval {
//     time_t      tv_sec;     /* seconds */
//     suseconds_t tv_usec;    /* microseconds */
// };
#[repr(C)]
#[derive(Debug)]
struct Timeval {
    tv_sec: time_t,
    tv_usec: suseconds_t,
}

// struct timezone {
//     int tz_minuteswest;     /* minutes west of Greenwich */
//     int tz_dsttime;         /* type of DST correction */
// };
#[repr(C)]
#[derive(Debug)]
struct Timezone {
    tz_minuteswest: c_int,
    tz_dsttime: c_int,
}

extern &#34;C&#34; {
    // 上記で定義した型をFFIの型に使える。
    // int gettimeofday(struct timeval *tv, struct timezone *tz);
    fn gettimeofday(tv: *mut Timeval, tz: *mut Timezone) -&gt; c_int;
}

fn main() {
    unsafe {
        // Cによって初期化するメモリは`mem::zeroed`で確保できる。
        // もちろん、Rustの構造体の初期化構文も使える。
        let mut tv: Timeval = mem::zeroed();
        // あるいはNULLを渡したい場合は`ptr::null_mut`も使える。
        let tz: *mut Timezone = ptr::null_mut();
        let ret = gettimeofday(&amp;mut tv as *mut _, tz);
        if ret == -1 {
            println!(&#34;failure&#34;);
            return;
        }
        println!(&#34;{:?}&#34;, tv);
    }
}
```

===

# Fearless Concurrency

&lt;!-- .slide: class=&#34;center&#34; --&gt;

===
# Fearless Concurrency
------------------

* Rustではバグの心配なく並行処理が書ける
* データ競合が起きない
  + 借用のおかげ
* データの管理者がわかりやすい
  + 所有権のおかげ
* ミスを防ぐAPI設計
* 便利ライブラリなど

===
# Goとの違い
------------

* Goの「便利なパーツが揃ってる」とは違う
* Rustにgoroutineはない
  + async/awaitみたいに近いものはある
* Goにバグを防ぐ仕組みはない
  + 便利な道具で起きにくくはある
* 善し悪しというより設計の違い
  + システムプログラミング言語vsアプリケーション言語

===
# Rustで並行処理
-----------------

* `spawn` 関数でスレッドを作れる
  + 中身はOSスレッド

``` rust
use std::thread;

let handler = thread::spawn(|| {
    // thread code
});

handler.join().unwrap();
```

===
# 並行処理とライフタイム
----------------------

* スレッドに渡すデータには `&#39;static` が要求される

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=ab08caa85dc05937a53e862308f11a33)

``` rust
use std::thread;

let mut x = 0;
let handler = thread::spawn(|| {
    x += 1;
});

println!(&#34;{}&#34;, x);
handler.join().unwrap();
println!(&#34;{}&#34;, x);
```


===
# データ競合を防ぐ仕組み
----------------------------

* スレッド間で移動していいデータにはマークがついている
  + トレイトをマーカとして使う
* マーカがついていないデータを送ろうとするとコンパイルエラー
* `Rc` などのスレッドセーフでないデータ型にはマーカがついていない

===
# スレッドとRc
--------------

* `Rc` を別スレッドに渡そうとするとコンパイルエラー

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=99b14133ec329cac0511f3d8c6b71918)


``` rust
use std::thread;
use std::rc::Rc;

let x = Rc::new(&#34;data&#34;.to_string());
let handler = thread::spawn(move || {
    let x = x.clone();
    println!(&#34;{}&#34;, x);
});

println!(&#34;{}&#34;, x);
handler.join().unwrap();
println!(&#34;{}&#34;, x);
```


===
# スレッドとArc
--------------

* `Arc` （Atomic reference count）なら別スレッドに渡せる

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=5a09fcd00028b2c26b596371e65d26de)

``` rust
use std::thread;
use std::sync::Arc;

let x = Arc::new(&#34;data&#34;.to_string());
let y = x.clone();
let handler = thread::spawn(move || {
    println!(&#34;{}&#34;, y);
});

println!(&#34;{}&#34;, x);
handler.join().unwrap();
println!(&#34;{}&#34;, x);
```


===
# ロック
--------

* `Arc` だと値を変更できない
* 値を変更するにはアトミック型やロックが必要
* ロック: [`Mutex&lt;T&gt;`](https://doc.rust-lang.org/std/sync/struct.Mutex.html)
  + Mutexは守る値を明示するスタイル
* アトミック型: [`atmoic`](https://doc.rust-lang.org/std/sync/atomic/index.html)

===
# `Mutex`
---------

* `lock` したあとに値を変更できる
* `File` と同じく `unlock` はない
  + ライフタイムが終わったら自動でunlockされる

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=6fc46b52ba35c70eda4c1e791f6fc458)

``` rust
use std::thread;
use std::sync::{Mutex, Arc};

let x = Arc::new(Mutex::new(&#34;data&#34;.to_string()));
let y = x.clone();
let handler = thread::spawn(move || {
    let mut lock = y.lock().unwrap();
    *lock = &#34;modified&#34;.to_string();
    println!(&#34;{}&#34;, lock);
});

handler.join().unwrap();
let lock = x.lock().unwrap();
println!(&#34;{}&#34;, x);
```

===
# デッドロック
--------------

* Rustでもデッドロックは防げない
* Rustが防げるのは同じメモリを読み書きして起きるエラー
  + = データ競合

===
# チャネル
----------

* スレッド間でデータを送る仕組み
  + メモリを共有するよりバグりづらいとされる
* Rustでは所有権ごと別のスレッドに送る

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=24c6f7aff9d4241be74fabe5c568b7d1)

``` rust
use std::thread;
use std::sync::mpsc::channel;

// Create a simple streaming channel
let (tx, rx) = channel();
thread::spawn(move|| {
    tx.send(10).unwrap();
});
assert_eq!(rx.recv().unwrap(), 10);
```

===
# チャネルとSend
----------------

* `spawn` と同じく `Send` でないデータは送れない

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=66d0da55e9ebbe77ab66c5f4570e28f4)

``` rust
use std::sync::mpsc::channel;
use std::thread;
use std::rc::Rc;

let (tx, rx) = channel();
thread::spawn(move || {
    let rc = Rc::new(1);
    tx.send(rc.clone()).unwrap();
});
assert_eq!(rx.recv().unwrap(), Rc::new(1));
```

===

# チャネル小話
--------------

* Goのチャネルとは違う
  + スケジューラが絡まなに
* Rustのチャネルはいくつか種類がある
  + bounded/unbounded
  + ブロックする/しない



===

# spawnの中身
---------------

* `spawn` は言語組込み **ではない**
    + C FFIで呼んでる
    ``` rust
    // UNIX系OSの場合
    pub struct Thread {
        id: libc::pthread_t,
    }
    ```
    + [spawnの中身](https://doc.rust-lang.org/src/std/sys/unix/thread.rs.html#43-91)
* 言語組み込みで安全なのではなく、安全に作る仕組みで安全に作ってる
  + → 他のFFIでもRustを通して使うと安全にすることができる

===
# コード例
----------

* IOスレッドを立ててチャネルで書き込むデータを送る

[Run](https://play.rust-lang.org/?version=stable&amp;mode=release&amp;edition=2018&amp;gist=b7818e1251990858fe11eaccf2a7351c)

``` rust
use std::fmt::Display;
use std::io::{prelude::*, stdout};
use std::sync::mpsc::{channel, Receiver};
use std::thread::{self, spawn, JoinHandle};

fn start_writer&lt;T: Display + Send + &#39;static&gt;(rx: Receiver&lt;T&gt;) -&gt; JoinHandle&lt;()&gt; {
    spawn(move || {
        let stdout = stdout();
        let mut out = stdout.lock();
        for data in rx {
            match writeln!(out, &#34;{}&#34;, data) {
                Ok(()) =&gt; (),
                Err(e) =&gt; {
                    eprintln!(&#34;Error: {}&#34;, e)
                }
            }
        }
    })
}

fn main() -&gt; thread::Result&lt;()&gt; {
    let (tx, rx) = channel();
    let handle = start_writer(rx);
    if let Err(e) = tx.send(&#34;hello&#34;) {
        eprintln!(&#34;Error while sending: {}&#34;, e)
    }
    {
        let tx = tx;
    }
    handle.join()?;
    Ok(())
}
```

===
# 並行性とライブラリ
--------------------

* 実際のところ自分でスレッドを作ることはそんなにない
  + もちろん、場面による
* 大抵はライブラリを使う
  + [rayon](https://crates.io/crates/rayon) など
* 生のスレッドよりも非同期プログラミングが主流
  + `async` / `await`

===
# Rayon
-------

* Rustの並行プログラミングライブラリ
* データ並列を気軽に書ける

[Run](https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=3e4e6f7ddb084666c55ef0e397f81b2f)

``` rust
use rayon::prelude::*;
fn sum_of_squares(input: &amp;[i32]) -&gt; i32 {
    input.par_iter() // &lt;-- just change that!
         .map(|&amp;i| i * i)
         .sum()
}
```

===
# コード例
----------

* CSVを読み込んで検索
* Rayonで並列化
  + （実際は検索処理が軽すぎて並列化しない方が速い）

``` rust
use rayon::prelude::*;
use regex::Regex;
use std::io::{prelude::*, stdout, BufWriter};
use std::sync::mpsc::{channel, Receiver};
use std::thread::{spawn, JoinHandle};
use std::{env, fmt::Display};

fn start_writer&lt;T: Display + Send + &#39;static&gt;(rx: Receiver&lt;T&gt;) -&gt; JoinHandle&lt;()&gt; {
    spawn(move || {
        let stdout = stdout();
        let mut out = BufWriter::new(stdout.lock());
        for data in rx {
            match writeln!(out, &#34;{}&#34;, data) {
                Ok(()) =&gt; (),
                Err(e) =&gt; {
                    eprintln!(&#34;Error: {}&#34;, e)
                }
            }
        }
    })
}

fn main() {
    let file = env::args().nth(1).expect(&#34;arg: FILE&#34;);

    let re = Regex::new(&#34;[rR]ust&#34;).expect(&#34;Regex syntax error&#34;);
    let (tx, rx) = channel();
    let handle = start_writer(rx);

    let rdr = csv::Reader::from_path(file).expect(&#34;failed to open input file&#34;);

    rdr.into_records()
        .par_bridge()
        .for_each_with(tx, move |tx, rcd| {
            let rcd = rcd.expect(&#34;failed to parse line&#34;);
            let text = &amp;rcd[5];
            if re.is_match(text) {
                tx.send(text.to_string()).expect(&#34;failed to send data&#34;);
            }
        });

    handle.join().expect(&#34;thread join error&#34;);
}

```

===
# 非同期プログラミング
----------------------

* Rustには `async` / `await` 構文がある
  + 協調的マルチタスク
  + スレッドより細かい単位でタスクを管理できる
* 言語側は構文だけ提供して、ランタイムはライブラリが担当
  + 言語本体はランタイムレスのまま
  + 組み込みとかでも `async` / `await` が使える

===
# 同期の問題点
---------------------------

* 他にやりたいことがあってもスレッドが止まる

``` rust
fn sync_do() -&gt; Result&lt;(), io::Error&gt; {
    write(b&#34;Hello, &#34;)?; // &lt;- ここでスレッドが止まる
    write(b&#34;World&#34;)?;   // &lt;- ここでスレッドが止まる
    Ok(())
}
```

===
# `async` / `await`
--------------------

* いくつかの変更で同期処理を非同期処理に書き換えられる
* 裏ではものすごい変換が行なわれている

[Run](https://play.rust-lang.org/?version=stable&amp;mode=release&amp;edition=2018&amp;gist=5c63fd079d135c42090ef60b7e856529)

``` rust
// async fn
async fn async_do() -&gt; Result&lt;(), io::Error&gt; {
    // async専用関数
    async_write(b&#34;Hello, &#34;).await?; // &lt;- await構文
    async_write(b&#34;World&#34;).await?;
    Ok(())
}
```


===
# 非同期についてもっと
--------------------------

* 正直、巨大すぎるトピックで全部は説明できない
  + そもそも非同期プログラミングが難しい
  + Rust特有のライフタイムとかランタイムとかも絡む
* [Async Book](https://rust-lang.github.io/async-book/01_getting_started/01_chapter.html)などを参考に


===
# まとめ
--------

* Rustはあたらしいシステムプログラミング言語
* システムプログラミング言語ながら高級な機能もある
* Rustを使うとある種の並行性由来のバグをコンパイルエラーにできる
* Rustには並行性を上手く扱ってくれるライブラリが揃ってる

&lt;/textarea&gt;</description>
    </item>
    
    <item>
      <title>言語自作を通して学んだRust</title>
      <link>https://KeenS.github.io/slide/gengojisakuwotoushitemanandarust/</link>
      <pubDate>Mon, 26 Oct 2020 21:24:22 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/gengojisakuwotoushitemanandarust/</guid>
      <description>Rustに入門してはじめてまとまった量のコードを書いたのが自作のLispインタプリタでした。当時の自分にはインタプリタは複雑すぎる題材でしたが、それゆえに多くのことを学びました。その後もシェルやMLコンパイラなどいくつかの言語を作っていきながらRustの機能を学んできました。本講演では言語自作を通して学んできたことをベースに、Rustに入門したての段階でのありがちなトラブルや意外と知られていない機能、Rustコンパイラの進化などを紹介します。Rustの入門体験記は色々ありますが、1つのテーマに沿って何度もプログラムを書いたことで徐々に問題へのアプローチが上手くなっていく点や、言語実装者が新たな言語を学んでいくという側面にも触れられたらなと思います。</description>
    </item>
    
    <item>
      <title>Rustで作るインメモリキャッシュ</title>
      <link>https://KeenS.github.io/slide/rustdetsukuruinmemorikyasshu/</link>
      <pubDate>Sun, 18 Oct 2020 18:22:00 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/rustdetsukuruinmemorikyasshu/</guid>
      <description>shinjuku.rsでの発表用。</description>
    </item>
    
    <item>
      <title>「安全」な言語Rust</title>
      <link>https://KeenS.github.io/slide/anzennagengorust/</link>
      <pubDate>Thu, 25 Jun 2020 09:31:42 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/anzennagengorust/</guid>
      <description>フィンテックエンジニア養成勉強会9での発表用。Rustの紹介。</description>
    </item>
    
    <item>
      <title>rust-analyzerの紹介</title>
      <link>https://KeenS.github.io/slide/rust_analyzernoshoukai/</link>
      <pubDate>Mon, 27 Apr 2020 23:38:53 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/rust_analyzernoshoukai/</guid>
      <description>下町.rsでの発表用。rust-analyzerについて。</description>
    </item>
    
    <item>
      <title>話題のGUIツールキットOrbtkを読む</title>
      <link>https://KeenS.github.io/slide/wadainoguitsu_rukittoorbtkwoyomu/</link>
      <pubDate>Tue, 10 Dec 2019 23:00:58 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/wadainoguitsu_rukittoorbtkwoyomu/</guid>
      <description>Rust LT #7 での発表用。GUIツールキットOrbtkのコードの軽い解説</description>
    </item>
    
    <item>
      <title>安全なシステムプログラミング言語Rustへの招待</title>
      <link>https://KeenS.github.io/slide/anzennashisutemupuroguramingugengorusthenoshoutai/</link>
      <pubDate>Mon, 18 Nov 2019 15:28:52 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/anzennashisutemupuroguramingugengorusthenoshoutai/</guid>
      <description>IIJ Labでの発表用。主にシステムプログラミング言語経験者向けの内容</description>
    </item>
    
    <item>
      <title>その正規表現エンジン、インタプリタで満足してる？！</title>
      <link>https://KeenS.github.io/slide/sonoseikihyougenenjin_intapuritademanzokushiteru__/</link>
      <pubDate>Fri, 18 Oct 2019 02:47:52 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/sonoseikihyougenenjin_intapuritademanzokushiteru__/</guid>
      <description>Regex Festa での発表用。SATySFiの多段階計算を使って正規表現をコンパイルする話</description>
    </item>
    
    <item>
      <title>Idrisの話とIdris2のウワサ</title>
      <link>https://KeenS.github.io/slide/idrisnohanashitoidris2nouwasa/</link>
      <pubDate>Sat, 21 Sep 2019 22:11:57 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/idrisnohanashitoidris2nouwasa/</guid>
      <description>Proof Summit 2019での発表用。Idrisと依存型と証明</description>
    </item>
    
    <item>
      <title>Effective Idris: Effects</title>
      <link>https://KeenS.github.io/slide/effective_idris__effects/</link>
      <pubDate>Sat, 25 May 2019 15:27:24 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/effective_idris__effects/</guid>
      <description>effect system勉強会での発表用。EffectのHandlerを書く話</description>
    </item>
    
    <item>
      <title>Rustから扱うAWS API</title>
      <link>https://KeenS.github.io/slide/rustkaraatsukauaws_api/</link>
      <pubDate>Tue, 12 Mar 2019 00:23:17 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/rustkaraatsukauaws_api/</guid>
      <description>Shinjuku.rs #3 での発表用。rusotoについて</description>
    </item>
    
    <item>
      <title>void</title>
      <link>https://KeenS.github.io/slide/void/</link>
      <pubDate>Wed, 16 Jan 2019 08:35:35 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/void/</guid>
      <description>&lt;textarea data-markdown
    data-separator=&#34;\n===\n&#34;
    data-vertical=&#34;\n---\n&#34;
    data-notes=&#34;^Note:&#34;&gt;
# void
----------------------
[Shinjuku.rs #2 @FORCIA](https://forcia.connpass.com/event/110888/)
&lt;!-- .slide: class=&#34;center&#34; --&gt;
===
# About Me
---------
![κeenのアイコン](/images/kappa.png) &lt;!-- .element: style=&#34;position:absolute;right:0;z-index:-1&#34; width=&#34;20%&#34; --&gt;

 * κeen
 * [@blackenedgold](https://twitter.com/blackenedgold)
 * Github: [KeenS](https://github.com/KeenS)
 * GitLab: [blackenedgold](https://gitlab.com/blackenedgold)
 * [Idein Inc.](https://idein.jp/)のエンジニア
 * Lisp, ML, Rust, Shell Scriptあたりを書きます
===
# 発散型
--------

* `!`
* [ドキュメント](https://doc.rust-jp.rs/the-rust-programming-language-ja/1.6/book/functions.html#%E7%99%BA%E6%95%A3%E3%81%99%E3%82%8B%E9%96%A2%E6%95%B0)
  + 標準ライブラリだと [exit](https://doc.rust-lang.org/std/process/fn.exit.html) とか
  + 他には無限ループ、パニック、 `return`/`continue` など
* 呼び出し元に制御が返らない
* どんな型にもなれる

===
# 発散型
--------

``` rust
use std::process::exit;
// pub fn exit(code: i32) -&gt; !

let hoge: i32 = exit(0);
let fuga: () = exit(0);
```

===
# `match` と型
--------------

* `match` の返り値は腕の型で決まる
* 発散型と他の型の組み合わせだと他の型になる

``` rust
let piyo: i32 = match 0 {
    // !
    0 =&gt; exit(0),
    // i32
    n =&gt; 1i32,
};
```

===
# `Void`
--------

``` rust
enum Void {}
```

===
# `Void`
--------

* バリアント(=コンストラクタ)がない
  + 値を作れない
* もしその値を受け取れるとしたらどうなる？

===
# `Void` の作り方
-----------------

* 発散型を使うと(コード上は)作れる
* 発散型なので実行時にはこのコードに到達しない

``` rust
let void: Void = unimplemented!();
//...
```

===
# `Void` の作り方2
-----------------

* 関数を使うと(コード上は)受け取れる
* 実際にはこの関数は呼べない

``` rust
fn take_void(void: Void) {
    // ...
}
```

===
# `Void` にマッチ
---------------

* Q: これはコンパイルが通る？
* A: 通る&lt;!-- .element: class=&#34;fragment&#34; data-fragment-index=&#34;1&#34; --&gt;

``` rust
fn absurd(void: Void) {
    match void {};
}
```

===
# `Void` にマッチ
---------------

* 返り値型はなんでもいい

``` rust
fn absurd(void: Void) -&gt; () {
    match void {}
}
```

===
# `Void` にマッチ
---------------

* 返り値を発散型にもできる!

``` rust
fn absurd(void: Void) -&gt; ! {
    match void {}
}
```

===
# `Void` と列挙型
-----------------

* `Result&lt;T, Void&gt;` という型を考える
* `Void` は作れないので `Err` はありえない
* `Ok` しか存在しない `Result` 型に

===
# `Void` と列挙型
-----------------

* Q: これはコンパイルが通る？
* A: 通らない&lt;!-- .element: class=&#34;fragment&#34; data-fragment-index=&#34;1&#34; --&gt;

``` rust
let result: Result&lt;(), Void&gt; = Ok(());
match result {
  Ok(()) =&gt; //
}

```

===
# `absurd` の使いどころ
-----------------------

* これはコンパイルが通る

``` rust
let result: Result&lt;(), Void&gt; = Ok(());
match result {
  Ok(()) =&gt; //
  Err(void) =&gt; absurd(void),
}

```

===
# `absurd` の使いどころ
-----------------------

* こういうのも作れる

``` rust
fn safe_unwrap&lt;T&gt;(result: Result&lt;T, Void&gt;) -&gt; T {
    match result {
        Ok(t) =&gt; t,
        Err(void) =&gt; absurd(void),
    }
}
```

===
# まとめ
--------

* `!` は特殊な型だよ
* 空の列挙型 (`Void`) から `!` が作れるよ
* `Void` を使うと面白いことができるよ
* See also [voidクレート](https://crates.io/crates/void) - 今回の内容を実装してるやつ
* See also [never type](https://doc.rust-lang.org/std/primitive.never.html) と [RFC](https://github.com/rust-lang/rfcs/blob/master/text/1216-bang-type.md) - `!` を空の列挙型にする提案


&lt;/textarea&gt;</description>
    </item>
    
    <item>
      <title>イマドキと言われる言語機能について</title>
      <link>https://KeenS.github.io/slide/imadokitoiwarerugengokinounitsuite/</link>
      <pubDate>Fri, 11 Jan 2019 00:34:20 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/imadokitoiwarerugengokinounitsuite/</guid>
      <description>産業界で比較的最近導入されるようになった言語や言語機能は学術界では古くから研究されていたものも多くあるようです。 例えばRustやScalaにあるトレイト、Swiftにあるプロトコル、Goのgoroutineやチャネルなどは遡ると1970年代、80年代頃に提案された機能にあたります。 本発表では産業界でイマドキと言われる言語機能を紹介し、最先端の研究から実用されはじめるまでのギャップを覗こうと思います。 </description>
    </item>
    
    <item>
      <title>Common Lispの多値とその実装達</title>
      <link>https://KeenS.github.io/slide/common_lispnotachitosonojissoutachi/</link>
      <pubDate>Thu, 15 Nov 2018 22:55:10 +0900</pubDate>
      
      <guid>https://KeenS.github.io/slide/common_lispnotachitosonojissoutachi/</guid>
      <description>言語処理系勉強会での発表用</description>
    </item>
    
  </channel>
</rss>
